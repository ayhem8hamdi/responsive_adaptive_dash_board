import 'package:flutter/material.dart';

class CustomHorizLine extends StatelessWidget {
  const CustomHorizLine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color(0XFFF1F1F1),
      thickness: 1,
    );
  }
}
